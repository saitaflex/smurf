# Shared by everything that runs inside the Vercel Sandbox.
google-adk
supabase
pydantic
groq

# Verifier sub-agents (task 3)
playwright
httpx
Pillow

# Dev / test only -- the sub-agent suite runs against local fixtures.
pytest
